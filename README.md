# Pipeline Assignment

This is the pipeline assignment given by Subspace, where a queue is used to manage the in-between states of the processing workflow.

---

## How to Run the Project

1. Install dependencies:

```bash
npm i
```
2.Start the script:

```bash
npm start <company-domain-without-https>
```
For example:

```bash 
npm start stripe.com
```